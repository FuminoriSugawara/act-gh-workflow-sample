const sqrt = (input) => {
  return Math.sqrt(input);
};

const pi = () => {
  return Math.PI;
};

// 二乗
const square = (input) => {
  return Math.pow(input, 2);
};

module.exports = {
  sqrt,
  pi,
  square,
};
